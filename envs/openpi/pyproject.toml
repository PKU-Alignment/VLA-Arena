[project]
name = "vla-arena-env-openpi"
version = "0.0.0"
requires-python = "==3.11.*"
dependencies = [
    "vla-arena[openpi]",
]

[tool.uv]
override-dependencies = ["ml-dtypes==0.4.1", "tensorstore==0.1.74"]

[tool.uv.sources]
vla-arena = { path = "../..", editable = true }
